export const REDIRECT_LOGIN = {
  redirect: {
    destination: '/login',
    permanent: false,
  },
};

export const REDIRECT_NOT_FOUND = {
  redirect: {
    destination: '/not-found',
    permanent: false,
  },
};
